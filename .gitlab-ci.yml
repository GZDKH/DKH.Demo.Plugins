include:
  - project: 'gzdkh/infra/DKH.Infrastructure'
    file: '/ci-cd/gitlab/openspec-validation.gitlab-ci.yml'

stages:
  - validate

# GitLab does not create a pipeline containing only .pre/.post jobs.
openspec:validate:
  stage: validate
  rules:
    - when: on_success
